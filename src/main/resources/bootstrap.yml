#Server port
server:
  port: 2001

#Git repo location.
spring:
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SahloulIheb/easyms-config-repo
          clone-on-start: true