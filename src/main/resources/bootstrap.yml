#Server port
server:
  port: 2001

#Git repo location.
spring:
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/easyms/easyms-config-repo
          clone-on-start: true